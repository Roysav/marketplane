server:
  address: "127.0.0.1:50051"

ledger:
  storage:
    postgres:
      connectionUri: "postgresql://user@localhost:5432/marketplane"

records:
  storage:
    postgres:
      connectionUri: "postgresql://user@localhost:5432/marketplane"

events:
  storage:
    redis:
      connectionUri: "redis://localhost:6379/0"

ticks:
  storage:
    redis:
      connectionUri: "redis://localhost:6379/1"
